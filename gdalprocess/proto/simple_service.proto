syntax = "proto3";

package gdal_dataset_service_simple;

service GdalDatasetService {
    rpc LoadTileData (RequestTileData) returns (TileDataReply) {};
}

message RequestTileData {
}

// for now just info about the success
message TileDataReply {
  bytes data = 1;
}
